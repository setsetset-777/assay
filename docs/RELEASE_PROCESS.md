# Release process

## RC 1

The first release candidate should allow a contributor to configure a form to ask its client for input and access this input.

#### The contributor can configure a form
- The contributor can save a form configuration
- The form can have multiple fields
- A field can have:
  - A title
  - A description
  - An input
- An input can be:
  - A short input
  - A text field
  - A select of multiple options
  - A group of mutliple checkobxes

#### THe client can use a form
- The client can access the form online
- The client input is saved as it is added

#### The contributor can acces the form input
- The form data is persistent accross browsers

## RC 2

In the second release candidates, an instance of the tool should have an administration tool and allow many contributors to configure many projects for many clients. An instance must have an adminstrator contributor who has access to all instance content.

### An instance has an administration tool
- Am administrator contributor can be created on instance first connexion
- Contributors can be defined as administrator
- Administrators can add/edit/remove contributors

### Forms are related to projects
- Contributors can crete/edit/remove projects
- A project can be associated to:
  - a title
  - a description
  - many forms

### Forms are private
- A contributor can invite a client to edit a form
- Accessing a form needs authentication
- Only a authorized client can edit the form
- A contributor can not edit the form

## RC 3

In the third release candidate, collaborative documents can be added to a project.

## RC 4

To be defined.
